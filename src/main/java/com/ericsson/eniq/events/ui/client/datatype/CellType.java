package com.ericsson.eniq.events.ui.client.datatype;

/*New Cell types introduced to improve usability, with more than text or links,
for now using simple enum to cut down duplication*/

public enum CellType {
    REGULAR_CELL,
    EVENT_CELL,
    TREND_CELL;
}
